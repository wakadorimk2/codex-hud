# Codex HUD 開発ガイドライン

## プロジェクトの目的

`codex-hud` は、Windows上で動作するCodexセッション監視HUDです。

Codex DesktopまたはCodex CLIを別の作業と並行して利用するときに、各セッションの現在状態を持続的に表示します。

このプロジェクトの最初の目的は、Codexがユーザー入力または承認を待っていることを外部から検知し、その状態を保持できるか確認することです。

一時的なトースト通知だけに依存しません。

## 基本方針

### ProbeをUIより先に成立させる

状態取得の成立確認を、UI実装より先に行います。

最初のマイルストーンはHUDの完成ではありません。

Codexが`WaitingForUser`または`WaitingForApproval`になったことを外部から検知し、セッション単位の現在状態として保持できることが最初のマイルストーンです。

### 観測を推測より優先する

Codex内部の未確認仕様に依存しません。

不明な挙動は、観測、時刻、入力操作、根拠となるイベントを記録してから設計判断を行います。

プロセスが存在することだけで、セッションが`Running`であるとは判断しません。

ファイルの無更新だけで、セッションが`Completed`または`Error`であるとは判断しません。

### 小さな縦切りで検証する

最初は一つのCodexセッションを読み取り専用で観測します。

次に、状態の正規化、状態保持、複数セッション、HUD表示の順に範囲を広げます。

各縦切りは、観測結果と成功条件を確認してから次へ進めます。

### 実験コードとプロダクションコードを分離する

Probeの実験コードは、観測方法を検証するための一時的なコードとして扱います。

実験コードをそのままHUDやプロダクションコードへ移しません。

実験結果、未確認事項、採用した根拠を文書に記録してから、必要な最小実装だけを移します。

## 責務の分離

| コンポーネント | 責務 | しないこと |
| --- | --- | --- |
| Codex | 既存のセッション実行とユーザー対話 | HUDのために利用方法を変更しない |
| Codex State Probe / Monitor | Codex固有の情報源から観測し、状態と根拠を正規化する | UIを描画しない。Codexをラップまたは操作しない |
| Session State Store | セッションごとの現在状態、遷移、根拠を保持する | Codex固有のファイル形式を直接解釈しない |
| HUD | Storeの現在状態を小さく持続表示する | `.codex`、App Server、CLI出力を直接読まない |

状態の正は通知イベントではなく、Session State Storeが保持する現在状態です。

Probeの検知方法が変わっても、HUDは正規化された状態だけを読みます。

## 開発の優先順位

1. 既存のCodex利用方法を変更せず、読み取り専用の観測方法を確認します。
2. `Unknown`、`Running`、`WaitingForUser`、`WaitingForApproval`、`Completed`、`Error`を定義します。
3. 状態と判断根拠をセッション単位で保持します。
4. 状態遷移と複数セッションを検証します。
5. Passive modeとExpanded modeを持つHUDを作ります。
6. 必要性を確認した後に、通知音と対象ウィンドウへの移動を検討します。

将来AI Agent Dashboardへ拡張できることを理由に、現時点で巨大な共通基盤、汎用プラグイン基盤、複数エージェント対応層を作りません。

## Windows前提

本プロジェクトはWindowsを対象とします。

Always on top、HWND、プロセスとの対応、ウィンドウフォーカス、DPI、複数モニター、全画面ゲーム、通知音をWindows固有の検討事項として扱います。

`.NET`、`WPF`、`WebView2`、Win32 APIの採用は、Probeの成立条件と実験結果を確認してから決定します。

## 現在のスコープ外

次の機能は現時点で対象外です。

- Codex自体の代替UI
- Codexの実行管理
- HUDからの承認操作
- HUDからのCodex操作
- ChatGPT、Claude、その他のAIエージェント対応
- モバイル通知
- 複数PC同期
- セッション履歴分析
- 生産性分析
- タスク管理
- AI Agent orchestration

## 作業と報告の規則

- 変更前に関連する設計文書を読みます。
- 事実、推測、不明点を分けて記録します。
- 同じ状態、コンポーネント、イベントには同じ用語を使います。
- 技術的な報告は短い文で書きます。
- ユーザー向けの報告は日本語で、お嬢様言葉と絵文字を使いますわ😊
- 未確認のCodex内部仕様を、確定した仕様として書きません。
